<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.APM"
        tools:targetApi="31">

        <activity
            android:name="me.ztiany.apm.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="me.ztiany.apm.scene.memory.MemorySceneActivity" />
        <activity android:name="me.ztiany.apm.scene.memory.MemoryLeakActivity" />
        <activity android:name="me.ztiany.apm.scene.memory.MemoryShakeActivity" />

        <activity android:name="me.ztiany.apm.scene.bitmap.BitmapSceneActivity" />
        <activity android:name="me.ztiany.apm.scene.bitmap.ExcessiveBitmapSceneActivity" />
        <activity android:name="me.ztiany.apm.scene.bitmap.BitmapMonitorSceneActivity" />

        <activity android:name="me.ztiany.apm.scene.fluency.FluencySceneActivity" />
        <activity android:name="me.ztiany.apm.scene.fluency.ListFluencySceneActivity" />

        <activity android:name="me.ztiany.apm.scene.throttle.ThrottleClickActivity" />
    </application>

</manifest>