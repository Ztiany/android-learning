<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
    If you prefer to disable automatic content protection on Android 10 (API level 29) or higher,
    set android:enforceNavigationBarContrast, android:enforceStatusBarContrast, or both to false in your theme.
    -->
    <style name="AppTheme.NoActionBar.EdgeToEdge">
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Optional: set to transparent if your app is drawing behind the status bar. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Optional: set the status bar light and content dark. -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

</resources>